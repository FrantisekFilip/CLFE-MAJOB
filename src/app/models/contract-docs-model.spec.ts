import { ContractDocsModel } from './contract-docs-model';

describe('ContractDocsModel', () => {
  it('should create an instance', () => {
    expect(new ContractDocsModel()).toBeTruthy();
  });
});
